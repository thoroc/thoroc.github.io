/** Sentinel key for "no primary language" repos, used across filters, galaxy
 * layout, and language-keyed lookups (colors, legend, chips). Locale-neutral
 * by design — display text comes from `messages.ts`'s `otherLang` key. */
export const OTHER_LANGUAGE_KEY = '__other__'
