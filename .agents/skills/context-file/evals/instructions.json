{
  "instructions": [
    {
      "type": "anti-pattern",
      "why_given": "new knowledge",
      "content": "NEVER create a .context/ file without the full YAML frontmatter block (title, type, status, date) — the pre-commit hook will block and the index cannot include the file."
    },
    {
      "type": "anti-pattern",
      "why_given": "new knowledge",
      "content": "NEVER include `related: []` when there are no related files — omit the key entirely instead of having an empty list."
    },
    {
      "type": "procedure",
      "why_given": "new knowledge",
      "content": "Always regenerate .context/index.yaml after creating or updating a .context/ file using .agents/skills/context-index/regenerate-context-index.sh."
    },
    {
      "type": "anti-pattern",
      "why_given": "new knowledge",
      "content": "NEVER put findings or plans under .context/audits/ — that directory is owned by skill-auditor --store."
    },
    {
      "type": "procedure",
      "why_given": "preference",
      "content": "Set `status: draft` on creation; promote to `active` only when reviewed."
    }
  ]
}
